# STATE — Flightdeck

Updated: 2026-07-20 (build session 8, local).

## What this is

The Flightdeck app build. Authoritative specs in `D:\Dev\ai\research\`:
`flightdeck-build-spec.md` (entry point) → `flightdeck-architecture.md`, `flightdeck-build-plan.md`, `flightdeck-design-spec.md`, `flightdeck-ui-mock.html`.
Read the build spec first on resume. This STATE.md is the live build dashboard.

## Current state

- **Rust installed**: rustup, stable-x86_64-pc-windows-msvc, cargo 1.97.1. Installed with `--no-modify-path`, so cargo is NOT on the global PATH — prepend `%USERPROFILE%\.cargo\bin` in every shell that runs cargo/tauri.
- **Project scaffolded**: Tauri 2 + React 19 + Vite 7 + TS, at `projects\active\flightdeck`. Node deps + `@xterm/xterm` + `@xterm/addon-fit` installed. productName/title = Flightdeck, window 1200x800.
- **Phase 0 spike code written** (not yet verified running):
  - `src-tauri/src/lib.rs`: PTY host on `portable-pty` (ConPTY). Commands `pty_spawn/pty_write/pty_resize/pty_kill`; events `pty://output` (base64) + `pty://exit`; per-pane reader thread; strips cross-vendor API keys, inherits the rest of the env (so ~/.claude etc. resolve as in VS Code). Claude launches via `pwsh -Command claude`; agy via `agy.exe --add-dir <cwd> --new-project --model gemini-3-pro`.
  - `src/Pane.tsx`: xterm.js terminal bound to a PTY (spawn on mount, stream output, forward keystrokes + resize, kill on unmount).
  - `src/App.tsx` + `src/App.css`: minimal Onyx-styled cockpit with an "add pane: pwsh / claude / antigravity" toolbar and an auto-grid. cwd hard-set to `D:\Dev\ai\Harness` for testing.
- **PHASE 0 SPIKE PASSED (2026-07-19)**: app compiles + runs. `pwsh`, `claude`, `agy` all render live and interactive on subscription auth (user signed in to each). **agy IS interactive in a ConPTY — the biggest risk is retired; no Electron fallback needed.** Colour fix applied (FORCE_COLOR/COLORTERM/CLICOLOR_FORCE on child env) — Node CLIs were monochrome without it. `npm run tauri dev` is running with a file watcher (edits to src-tauri auto-recompile + restart the window).

## Next steps — Phase 1 (per build-spec §4)

**DONE 2026-07-19 (session 3) — full Deep Cove re-skin IMPLEMENTED.** Claude Design returned the system (project `34702292-...`, file "Flightdeck Design System.dc.html"); handoff brief was at `research\flightdeck-design-handoff.md`. Implemented:
- `src\theme.css` (new) = single token source, dark `:root` + light `:root[data-theme="light"]`. App.css `:root` now only *aliases* legacy names (`--bg-app`→`--bg`, `--tmuted`→`--faint`, etc.) onto it; Board.css local colour block deleted (inherits). No more token duplication.
- `src\fonts.css` (new) + `src\assets\fonts\` = 7 bundled woff2 (Schibsted Grotesk 400/500/600/700 + JetBrains Mono 400/500/600, latin, self-hosted, offline-safe). Killed `Plus Jakarta Sans` (that was the "shimmery" text — never installed, was falling back). Fallback stack = Segoe UI Variable.
- `src\Icons.tsx` rewritten (20 icons, kept `size` prop + `icon-bell` hook). `src\terminal-theme.ts` (new) wired into Terminal.tsx (xterm stays dark in both app themes).
- Pane 3px status top-band (`.pband`) + header dot; premium Kanban (gradient New-task btn, per-card left priority stripe, brand column-accent dots, SVG glyphs); Board colours are token-based so they flip in light mode.
- App icon pack regenerated via `tauri icon` from `src-tauri\icons\icon-master.svg` (swept-delta wing in the Deep Cove gradient).
- Theme toggle in the top bar (half-disc icon) → flips `data-theme` on `<html>`, persisted to `localStorage['flightdeck-theme']`, applied on boot in main.tsx.
- Verified: `npm run build` clean, `npm test` (new vitest, `src\store.test.ts`) 9/9 pass. Reviewer + a Fable design-fidelity pass run; all their findings fixed (Kanban light-theme contrast, `.btn-primary`→gradient, focus rings standardised to `--ice`, localStorage guarded, richer pane-grid empty state).
- NOT yet eyeballed by Balu in the WebView. Dev app is live (HMR'd + Rust recompiled).

**DONE 2026-07-19 (session 3) — bugs fixed from the Fable architecture/bug pass (all blocker + major):**
1. **Orphaned processes on app close (blocker):** `lib.rs` `run()` now uses `.build().run(|_,e|)` with a `RunEvent::ExitRequested` handler that `reap_pane`s every live pane's process tree — closing the window no longer leaves claude/agy/pwsh trees running. (`reap_pane` extracted from `pty_kill`; still taskkill /T the tree. Job Object for hard-crash coverage = future hardening, noted.)
2. **Silent spawn failure + false "running" (blocker):** `Terminal.tsx` wraps `pty_spawn` in try/catch → writes an error line + sets pane `error`. New honest initial state `"starting"` (store.ts, added to `PaneState`) instead of optimistic `"running"`; reader flips it to `running` on first output. `.pband.starting`/`.pdot.starting` styled (azure pulse).
3. **Registry leak on natural exit (major):** the reader thread now prunes its pane from the `Registry` when the read loop ends (was only `pty_kill` from the UI) — no more leaked master/writer/child handles for panes that exit on their own.
4. **Crash never surfaced as error (major):** reader `try_wait()`s the child on EOF, sends `crashed` in `pty://exit`; `PaneView` sets `error` (red) on crash vs `idle` on clean exit.
5. **Early output dropped before pane id known (major):** `Terminal.tsx` buffers `pty://output` arriving while `paneId===0` and replays the matching chunks once spawn resolves.
Minors: added a TODO on the `CROSS_VENDOR_KEYS` allowlist (proxy/enterprise auth vars); `csp:null` + StrictMode dev double-spawn + resize-gap left as noted watch-items. Verified: frontend build clean, `npm test` 9/9, cargo rebuilt clean + app running.

**Recommended next (per Fable arch pass), before a daily-driver:** item 2 below (vendor adapter trait — the load-bearing refactor everything else sits on), then Windows Job Object hardening, then SQLite persistence (needs stable exit/error state first — now done). Full ranked plan captured from the pass.

**DONE 2026-07-19 (session 4) — PHASE 1 (feel & polish) complete + verified.** Full backlog is phased in `BACKLOG.md` (215 items across Phases 1-5; single-user scope, market polish kept, licensing/docs/team/sync/support parked). Phase 1 built:
- **Settings screen** — new `Settings.tsx`/`overlays.css`, ⚙ now opens it (also Ctrl+,). Theme (dark/light) + UI size (Comfortable/Large/Extra = whole-app `zoom`, persisted, boots in `main.tsx`) + keyboard reference. `ui.ts` = small UI store (confirm/toasts/settings flag).
- **Close-workspace confirm** — closing a workspace with live panes now prompts (`ConfirmDialog.tsx`, danger style) so a session isn't lost by accident; toast on close. Reusable confirm + `ToastHost.tsx`.
- **Left panel redesign** — bigger tiles/icons, per-workspace status roll-up (running/waiting/error dot-counts), larger board button.
- **Larger type + icons** app-wide (topbar, panel, board header); dark-mode polish (more surface separation + stronger lines in `theme.css` dark `:root`).
- **Kanban** — larger cards (min-height, padding, 13.5px title), stronger hover lift, wider columns, and a "Drop here" slot that appears in the target column while dragging a card in from another column.
- Shortcuts: Ctrl+, settings, Ctrl+B toggle panel (guarded so it doesn't steal keystrokes inside a terminal). Verified: `npm run build` clean, `npm test` 9/9. NOT yet eyeballed by Balu.
- **Phase 4 (macOS/Linux/signing/Store/auto-update) + Phase 5 (kove.nz pages) are gated** on Balu's hardware/certs/accounts — checklist only, can't be built/tested from here.

**DONE 2026-07-19 (session 4) — PHASE 2, wave 1 (verified: build clean, 9/9 tests, cargo clean, app running):**
- **R1 vendor registry** — `lib.rs` now has a data-driven `VENDORS` table (`VendorSpec{id,label}`) + `probe()` + `agy_path()`; `build_command` consumes it. Adding an agent = one row + a `build_command` arm + a `probe` arm (full trait/dyn-dispatch is a later refinement; this removes the hand-rolled if/else the arch pass flagged).
- **R3 first-run CLI detection** — new `detect_vendors` command probes each vendor (`where claude`, `%LOCALAPPDATA%\agy\bin\agy.exe`, `where pwsh`) and returns installed + resolved path. `NewWorkspace` calls it on mount and shows an amber **"not installed"** chip (with the probe detail as tooltip) next to any pane whose agent is missing. Never blocks launch — capability stays visible.
- **R5 crashed/idle pane relaunch** — `PaneModel.epoch` + `restartPane()` in the store; `PaneView` keys `<Terminal>` on `epoch`, so a bump remounts and respawns the PTY. A **Restart** button appears in the pane header whenever state is `idle` or `error`.
- **K5 resize race fixed** — `Terminal.tsx` pushes one `pty_resize` right after spawn, so a container resize during the spawn round-trip is no longer lost.

**DONE 2026-07-19 (session 4, wave 2) — 8 PARALLEL AGENTS, all integrated + verified** (cargo check clean, `npm run build` clean, vitest 9/9, `cargo test --lib` 3/3, app running).
- **rust-core**: backend split into modules (`vendors` trait+registry with claude/agy/pwsh/**cmd/git-bash/wsl**, `job` = Win32 Job Object w/ KILL_ON_JOB_CLOSE assigned at spawn (R2), `health` = pane_health CPU/mem, `orphans` = recover/kill stray agent trees, `support` = redact() + export_support_bundle (3 unit tests), `gitstatus` = git_status). CSP tightened (researched: with `devUrl` set, CSP only applies to production builds, so dev is unaffected). `dialog:default`→`dialog:allow-open`. Wired `persist.rs` + its 8 commands into `invoke_handler` — persistence commands are now LIVE.
- **persistence**: `persist.rs` + `persist.ts` — atomic JSON session doc, safe mode, restore points, backup/import. `PersistedPane` has NO state field by design (a restored pane is always dead).
- **kanban**: card→pane dispatch (R7), live pane status on cards, detail modal, labels, filter/sort, checklist, WIP limits, keyboard moves, markdown export, `board/` module + `boardStore`.
- **panes**: maximise/solo, drag-reorder, inline rename, portalled overflow menu, per-pane font zoom (mutates xterm options without respawning the PTY).
- **workspaces-nav**: reorder/rename/context-menu/search/Ctrl+1-9/folder-drop/last-active + **CommandPalette** (Ctrl+K/Ctrl+P).
- **settings-theme**: 6 themes (Deep Cove dark/light, Dracula, Gruvbox, Nord, High Contrast), accent picker, colour-blind palette, reduced motion, terminal/shortcut/agent/startup settings, theme import/export.
- **notify-agents**: configurable per-state bell, feed, mute/DND, OS toast + taskbar flash, **Broadcast bar**; extended `ui.ts`.
- **explorer-git**: `Explorer.tsx` file tree (behind a new top-bar toggle), git branch pill, open-in-editor, new-terminal-here.
- **Integration fixes I made** (agents couldn't reach these): lifted `activeView` into `ui.ts` so CommandPalette can leave the Board view; rerouted the topbar theme toggle through `themes.ts` so it no longer discards a Dracula/Nord choice; added `core:window:allow-request-user-attention` so taskbar flash works; rewrote `Cockpit.tsx` to mount Notifications/Broadcast/CommandPalette/Explorer.
- **GAP REVIEW written** — `BACKLOG.md` section I, items **216-284**. Top finding: the Rust vendor trait exists, but the frontend hardcodes the vendor list in 5 places and `board/types.ts` has a `Vendor` union type that structurally blocks new agents. **I1 (216-228) is the priority: make adding an LLM a config drop, not a code hunt.** Second-biggest: testing (only 9 TS + 3 Rust tests).

**DONE 2026-07-19 (session 5) — live foreground process name per pane (audit item, backend only).** New `procname.rs` (deepest-descendant-of-root-pid heuristic over a shared toolhelp snapshot, tie-break by pid; `.exe` stripped). `orphans::snapshot_processes` made `pub(crate)` and reused (no duplicate Win32 walk). `lib.rs`: one sampler thread (~2s tick, skips the walk entirely when there are no panes), emits `pty://proc {paneId, name}` only on change per pane, plus an immediate emission of the vendor's root name right after spawn. `health::PaneHealth` gained an additive `procName` field, filled from the same per-pane cached name. `cargo check` + `cargo test --lib` clean (14/14, 5 new). **Frontend not wired yet** — needs a `pty://proc` listener (mirror the existing `pty://state` listener pattern) to update pane header text, plus consuming `procName` from `pane_health` polls if that's already polled.

**DONE 2026-07-19 (session 4, wave 3 — Fable premium-audit follow-through, 6 parallel agents; verified: build clean, vitest 9/9, cargo test 14/14):**
- **Terminals follow the app theme** — `terminal-theme.ts` now has 6 xterm palettes (Deep Cove dark [also used in light — Kove rule: terminals stay dark], Dracula, Gruvbox, Nord, High-Contrast) + `terminalThemeFor()`; `Terminal.tsx` live-switches via MutationObserver on `data-theme`, no PTY respawn; search-highlight colours follow too.
- **Real git branch in pane headers** — polls `git_status` (mount/restart/30s); dirty dot; pill hidden for non-repos. No more hardcoded "main".
- **Live process name** — backend `procname.rs` (deepest-descendant walk off a shared toolhelp snapshot, 2s sampler, emits `pty://proc` only on change; root name emitted at spawn; `pane_health.procName`); frontend bridge Terminal `onProc` → PaneView `.pproc` mono chip. 5 new Rust tests (14 total).
- **Broadcast docked** — floating FAB removed; opens from a new topbar `IconBroadcast` button / palette / `useUI().broadcastOpen`; dead panes shown greyed with reason; sends recorded (`pushBroadcastRecord`, cap 20) + "last sent" line.
- **Explorer** — persisted drag-resize width (180-480, default 240) + open-state persisted via `ui.ts.explorerOpen`.
- **Launcher** — first-run screen now has chrome (brand + theme toggle + settings + overlays mounted pre-workspace); layout tiles 1/2/4/6 + custom 1-9 stepper.
- **Command palette** — close-pane (with live-session confirm), toggle-explorer, open-broadcast actions. **Settings > Shortcuts** now lists Ctrl+K/P + Ctrl+1-9 as read-only rows.
- Icons: `IconBroadcast`/`IconTerminalPlus`/`IconWipe` (board reset no longer shares the restart glyph); `.board-icon-btn` circle→6px square; light `--surface` #F7FAFD for real elevation contrast; CardDetail "Unlink pane".
- Parked (solo pass, collision-heavy): type-scale collapse to a ratio system; `--ease-soft` decision.
- **Demo video** — `demo/` (reel.html + Playwright recorder → `demo/out/flightdeck-demo.webm`) for the kove.nz showcase; produced motion-graphics recreation, NOT a screen capture; review before any site use. [status per agent report]

**DONE 2026-07-19 (session 4, wave 4 — ship-quality + VISUAL pass; verified: build clean, vitest 9/9, live-DOM probe):**
- **QOL-AUDIT.md written** (Fable, 100 items numbered 285-384, 15 ⭐, ship gate at the end). Biggest finding: 4 built+tested Rust commands with zero UI (pane_health, recover/kill_orphans, export_support_bundle, the whole persist.ts client).
- **Ship-gate fast items FIXED**: React ErrorBoundary (`ErrorBoundary.tsx`, wraps App — one render throw no longer blanks live sessions; proven working the same hour when the browser rig tripped it); hardcoded `D:\Dev\ai\Harness` default dir removed (placeholder now); Kanban seed replaced with a generic onboarding board (was Balu's personal backlog); Terminal now READS Settings>Terminal (fontFamily/cursorStyle/scrollback were persisted-but-ignored); Settings closes on Esc; About shows `APP_VERSION` 0.1.0; token-driven scrollbars across all 6 themes (theme.css); dynamic window title "Flightdeck — N waiting/errors" (guarded for non-Tauri).
- **Visual pass (Playwright rig `demo/visual-check.mjs` → screenshots → actually LOOKED)**: found + fixed (1) first-run launcher pinned top-left (`.cockpit>.launcher` needed flex:1), (2) cockpit render-crash in browser preview (`getCurrentWebview()` unguarded in LeftPanel — real app unaffected, now guarded), (3) **class collision: `lp-i board` matched Board.css's global `.board`**, inflating the icon to 211px and crushing the "Board" label to 0px — renamed `lp-board-i` (probe-verified 30px/172px), (4) pane bottom striping (`.pbody` now --abyss to match xterm). Rig is committed for future visual QA: `node demo/visual-check.mjs <outdir>` with the dev server running.
- **Add panes freely** (earlier this wave, commit eaf0ffc): topbar + button w/ vendor menu; empty-grid Add-a-pane button.
- **QOL still open**: the rest of QOL-AUDIT (285-384) esp. persistence wiring (378) + Settings>Diagnostics surfacing pane_health/orphans/support-bundle (375-377); board persistence; type-scale + ease-soft solo pass.
- **Demo reel v2 in flight** (Fable agent): premium rebuild (camera rig/cursor actor/spotlight, fixes the cut-off frame) with mandatory frame verification; commits separately when rendered.

**DONE 2026-07-19 (session 6, CLOUD — market research + TIER 0: worktree isolation + review; commit fcf717f; verified: cargo test 25/25, vitest 9/9, build clean. NOT yet run on Windows — E2E checklist below):**
- **Market research (Balu asked "what would going to market take").** Category = desktop cockpits for parallel AI coding agents. Closest competitors: **BridgeMind/BridgeSpace** (16-agent grids + agent-dispatching Kanban + swarm roles + shared memory + editor/browser — closest full-concept rival, big community moat; NB our dropped "BridgeSwarm" §F item is literally their product), **Paneflow** (OSS Rust/GPUI; worktree diff review, Attention Queue, read-only MCP bridge, token/cost), **Superset** ($30/mo, 10+ agents, diff editor + in-app browser), **Conductor** (Mac polish bar), **Warp 2.0**, Vibe Kanban, Claude Squad, Crystal; first-party threats: Claude Code Agent Teams/Dynamic Workflows, Cursor parallel agents, Codex, **Antigravity's own Agent Manager**, Copilot coding agent. **Category table-stakes we lacked: per-agent git worktree isolation + diff/merge review → BUILT (below).** Differentiators to lean into: local-first/no-account, I1 #218 config-drop agent manifest (**still unbuilt**), focused single-user polish. Docs correction: I1 216/217/227 had already shipped (Vendor union is `string`, UI reads `detect_vendors` registry) — earlier STATE/BACKLOG claims of "5 hardcoded lists" were stale.
- **TIER 0 backend — `src-tauri/src/worktree.rs`** (11 tests). Commands: `git_worktree_add` (idempotent create/reuse of `<app-data>/worktrees/<repo-hash>/<slug>`, branch `flightdeck/<slug>`, reattaches surviving branches; per-repo mutex vs index.lock races; sidecar `<dir>.meta.json` = repo/branch/baseBranch), `git_worktree_remove` (modes ask/keep/discard — keep commits outstanding work to the branch first; Windows handle-release retry), `git_worktree_gc` (launch-time reap of unclaimed worktrees; **keep-commits stray work to its branch before removing — crash-safe, never destroys agent work**), `git_repo_toplevel`, `git_diff_summary`/`git_file_diff` (numstat + per-file patch vs merge-base of recorded base; **untracked files included** via intent-to-add, only ever inside Flightdeck-owned worktrees; 400KB payload cap), `git_merge_back` (auto-commit squash → `--no-ff` merge into base; conflict aborts cleanly leaving branch intact; refuses dirty base / switched branch). New `git()` helper returns `{code,stdout,stderr}` untrimmed (gitstatus's `run_git` trims + swallows stderr — wrong for patches/conflicts). `Pane` + `PersistedPane` carry `worktreePath/branch/baseBranch` (schema landed while frontend persistence is still unwired — cheap window).
- **TIER 0 frontend.** `src/worktrees.ts` = single async spawn path (`spawnPane`/`preparePanes`) owning worktree creation in the user-action path (StrictMode double-mounts + epoch restarts REUSE, never re-create); routed through **all six spawn sites** (NewWorkspace, topbar add-pane, Board card dispatch, empty-grid add, Explorer new-terminal-here, LeftPanel folder-drop). Workspace **duplicate** re-prepares fresh worktrees (was about to share + double-free the original's). NewWorkspace: isolation toggle (persisted pref, auto-disabled w/ note for non-repos, async create w/ "Preparing worktrees…"). Close flows (pane header, palette, workspace) + boot GC clean up worktrees; dirty close prompts keep-on-branch. **`Review.tsx` drawer** (first side-sheet primitive, `review.css`, token-driven): per-file numstat list, unified patch w/ colored add/del/hunk lines + hunk prev/next, Merge-back w/ confirm + outcome toasts; opens from new per-pane **diff-stat badge** (`+adds −dels`), pane overflow menu, Ctrl+K. New icons: diff/merge/commit. **Explorer `git_status` invoke fixed (`{path}`→`{cwd}`)** — the branch pill had never rendered. WSL panes excluded from isolation (D12: `.git` file embeds a Windows gitdir path WSL git can't resolve).
- **E2E checklist for Balu (Windows):** create workspace on a real repo w/ isolation on → 2 agent panes → `git worktree list` shows 2 under `%APPDATA%` → edit + create files in pane A → badge/drawer show both (untracked incl.) → merge back clean → conflict path aborts w/ branch intact → close dirty pane → keep/discard prompt → relaunch: GC toast + `git worktree prune`d → **agy in a worktree: verify trust prompt behavior (D9 untested — `ensure_agy_trust` currently appends each worktree path to trustedWorkspaces; may want parent-dir trust)** → Explorer branch pill now renders.
- **Known deferrals (BACKLOG §J):** merge-conflict surface (v1 aborts w/ message), PR handoff flow, per-workspace worktree setup command (fresh worktrees have no node_modules — agents can't build until `npm ci`), per-pane Explorer rooting (Explorer still shows the main checkout, not the focused pane's worktree), MAX_PATH note (worktrees live under app-data; enable longPathsEnabled if deep node_modules trip it).

**DONE 2026-07-20 (session 7, CLOUD — R4 persistence + Balu's flagged picks; verified: build clean, vitest 11/11, cargo check clean. NOT yet run on Windows):**
- **Decisions recorded (BACKLOG):** macOS/Linux/ARM **parked** (Windows-first). Local LLM = **CLI experience, OpenCode frontrunner**, lower priority. Rename **parked** (§L shortlist: Moorage/Berth/Covework). Master UI list = BACKLOG §K (UI-1..51).
- **UI-50 custom accent (Balu-requested):** `deriveAccent(hex)` in themes.ts — any colour → derived dark/light ice/azure/accent/grad; rainbow swatch + `<input type=color>` in Settings; topbar theme toggle now re-applies the accent variant for the new mode.
- **R4 PERSISTENCE WIRED (378 closed):** new `session.ts` — debounced autosave of workspaces/panes (+worktree fields) via `save_session`; boot **"Reopen last session?"** prompt (Settings>Startup honoured: "reopen" auto-restores silently — that setting was persisted-but-inert); `hydrate` store action (id-counter bump, 2 new tests); **worktree reconcile** on restore (intact→keep, dir gone→reattach branch via idempotent add, repo gone→root fallback + toast); safe-mode banner. **Board persists too (229)** — cards ride `uiPrefs.board`, restored even if the workspace prompt is declined.
- **UI-44 quit guard (373):** `onCloseRequested` confirms when sessions are live → `destroy()` (cap `core:window:allow-destroy`); reads pane state at close-time.
- **UI-12:** "Launching <vendor>…" overlay on starting panes (`.plaunching`, panes.css).
- **UI-4 Diagnostics (375-377):** Settings section — pane-health table (3s poll), stray-agent scan/kill, support-bundle export via save dialog (cap `dialog:allow-save`).
- **#218 VENDOR MANIFESTS SHIPPED (the differentiator):** drop a JSON into `%APPDATA%/…/vendors/` → new launchable agent, no recompile. `ManifestVendor` adapter (id/label/short/kind/accent/exe/args/env/probe) merged into `registry()`; manifest `env` keys are explicit overrides exempt from the ambient strip (dummy `OPENAI_API_KEY` for LM Studio survives; all other BASE/PROXY keys still stripped — tested); accent = theme token OR hex (frontend `accentCss()` resolves both); `_example-opencode.json` auto-written on first run (wired for the OpenCode/LM Studio decision); Settings>Agents "Open vendors folder" button. 28/28 Rust tests.
- **Windows E2E additions for Balu:** relaunch app → reopen prompt restores workspaces + board + reattaches worktrees; quit via window X with live panes → confirm dialog; Settings>Diagnostics shows live CPU/mem; custom accent survives restart + theme flips; **rename `_example-opencode.json` → `opencode.json` (with OpenCode installed) → it appears in New Workspace and launches.**

**DONE 2026-07-20 (session 8, LOCAL — Balu's Windows E2E validation PASSED clean, then the 4-item resume queue; verified: cargo test 37/37, vitest 12/12, build clean, dev app live):**
- **Windows E2E validation (sessions 6-7 work): ALL PASSED.** Worktree create/review/merge, restart-restore, quit guard, manifests, colours, Diagnostics — no bugs found. Balu confirmed intent questions: files landing in the worktree (not the chosen folder) is the isolation working; worktrees are removed on pane/workspace close + boot GC, only working files are copied (object DB shared), so disk doesn't accumulate.
- **Worktree SETUP COMMAND (Tier 0 follow-up, K0c closed):** New Workspace "Worktree setup" field when isolation is on — suggested from the repo lockfile (`detect_setup_command`: npm ci / pnpm / yarn / bun), per-repo last-used remembered (localStorage `flightdeck-setup:<top>`); runs inside each FRESH worktree via a pwsh wrapper (`vendors::wrap_with_setup`) before the agent, output streams in the pane, failure exits the pane (error state) without starting the agent; manifest env survives the wrap; `Pane.needsSetup` consumed on spawn so Restart never re-runs `npm ci`; folder-drop + duplicate carry it; persisted (`PersistedWorkspace.setupCmd`) and restore re-runs setup when a worktree had to be recreated.
- **K0a agy trust FIXED:** worktree removal/GC prunes the dir from agy's `trustedWorkspaces` (file-parameterised helpers under the same lock, case/slash-insensitive); the stale entry from the morning's E2E run was found in the live settings.json (confirming the leak) and cleaned.
- **PR HANDOFF (merge path v2):** `git_pr_handoff` — auto-commit (shared `commit_outstanding` with merge_back) → `push -u origin` → compare-URL for GitHub/GitLab/Bitbucket opened via plugin-opener ("Create PR" secondary button in the Review drawer); statuses pushed / nothing-to-push / no-remote / push-failed; tested incl. a real push to a local bare origin.
- **#219 AUTH PROBE + RUN LOGIN:** `VendorAdapter::auth()` from credential-file presence (claude `.claude/.credentials.json`, agy `.gemini/google_accounts.json`, shells always ok, manifests optional `authFile`, files never read); `detect_vendors` carries authState/authDetail; "not signed in" chip in New Workspace; Settings > Agents shows signed-in/not-installed and "run login" spawns a pane so the CLI drives its own flow; vendors store gained `refresh()` (Settings re-probes on open).
- **Docs corrected:** R2 Windows Job Object was already shipped in wave 2 (`job.rs`, KILL_ON_JOB_CLOSE at spawn) — BACKLOG R2/K2 and the old "Phase 2 remaining" line here claimed it open.
- **UI-5 merge-conflict surface:** conflict shows in-drawer (file list from `--diff-filter=U` captured pre-abort, both-branches-intact note, "Create PR instead") — no more vanishing toast.
- **Per-pane Explorer rooting:** Workspace/Pane scope toggle when the focused pane is isolated; tree, git pill and new-terminal-here follow the pane's worktree; preference persisted.
- **FIRST PRODUCTION BUILD (unsigned, csp still null — R6 is its own pass):** `npm run tauri build` → `src-tauri\target\release\bundle\nsis\Flightdeck_0.1.0_x64-setup.exe` (+ .msi). Release exe smoke-tested (boots, stays up). Daily-driver plan: Balu installs this and works out of it; dev instance iterates; rebuild + reinstall when a batch goes green. Cosmetic: the raw binary is still `projectsactivefd-scaffold.exe` (cargo package name) — installer/app name are Flightdeck.

**DONE 2026-07-20 (session 8, batch 3 — verified: cargo 39/39, vitest 15/15, build clean):**
- **UI-1 v2 Attention Queue overlay** — `attention.ts` shared ranking (approval > error > waiting, longest-first) + `AttentionQueue.tsx` (Ctrl+Shift+A / palette / bell "See all"; arrows/1-9/Enter/Esc; live durations).
- **UI-3 token chip** — `usage.rs` reads Claude Code's own session transcripts (`~/.claude/projects/<cwd-slug>/*.jsonl`, incremental offset scan); PaneView "NNk ctx" pill w/ exact-number tooltip; transcript-less agents get no chip (honest by construction).
- **R6 status corrected:** CSP was ALREADY tightened in wave 2 (K3/R6 claims stale). Release exe boots under it; full visual verify pending a free screen. Capabilities reviewed — already minimal.
- **Dev-watcher etiquette:** dev instance + smoke launches KILLED mid-session after a screenshot showed a fullscreen game on the primary display — no GUI launches while Balu's screen is busy; CSP eyeball deferred.

**DONE 2026-07-20 (session 8, batch 4-11 — the §M polish sweep, 66 items; verified: cargo 43/43, vitest 30/30, build clean + warning-free, E2E 23/23):**
- **Foundation:** `src/format.ts` (one relTime/duration/absTime/num/compact/bytes — three drifted copies collapsed); motion/elevation/radius/pulse tokens; documented overlay z-scale. **Fixed 2 real layering bugs found while migrating:** the portalled pane menu sat at z-200 (above confirm dialogs AND toasts) and the Review drawer at z-60 (below Settings and the bell).
- **Perf:** `src/poll.ts` — `cachedInvoke` dedupes concurrent callers per (command,args); a 6-pane workspace on one repo went from 18 invokes/cycle to 3. Panes off-screen and a minimised window stop polling entirely. Bundle split: app chunk 933KB → 183KB, build now warning-free. pty writes batched per animation frame.
- **New Workspace:** recents, live path validation (warn-never-block), duplicate/remove/reorder slots, setup-source line, per-repo layout memory, same-folder collision warning, per-slot progress, drop-to-fill, Enter/Esc.
- **Panes/terminal:** double-click maximise, middle-click close, branch-copy, cwd-reveal, Ctrl+W / Ctrl+Shift+W / Alt+1-9, slow-start + setup-phase copy, attention chip, right-click menu, multi-line paste confirm, clear scrollback, BEL pulse.
- **Queue/notify:** last-output-line on rows, 10-min snooze, split bell badge (errors vs approvals), focus-mode auto-DND.
- **Review:** branch commit list, base-drift pill, update-from-base, copy patch, reopen PR.
- **Settings:** worktree inventory with sizes + orphan cleanup, manifest parse errors surfaced (were silently skipped).
- **Elsewhere:** left-panel mini pane-grid previews + worktree badges, Ctrl+Tab MRU, itemised quit guard, task-named branches (`flightdeck/fix-login-badge`), Explorer context menu + remembered expansion, palette state dots + bulk restart, broadcast history recall, a11y (OS reduced-motion default, forced-colors, 24px hit targets, focus rings).
- **E2E RIG: `demo/e2e-session8.mjs`** (run with the vite dev server up, from `demo/`: `node e2e-session8.mjs ../e2e-shots`). 23 assertions. **It caught 3 real bugs:** (1) NewWorkspace crashed the whole cockpit outside Tauri — `getCurrentWebview()` throws *synchronously* so `.catch()` never saw it (the exact trap that hit LeftPanel in wave 4); (2) **Esc couldn't close any overlay while a terminal had focus** — xterm stops propagation on Escape, so every overlay dismiss listener moved to the capture phase; (3) two of my own UI-218 hit-target violations.

**SESSION-8 TALLY: 107 of 201 pool items done, 94 open.** Gates at every commit: cargo 46/46, vitest 38/38, frontend build clean + warning-free, **E2E 24/24** (`demo/e2e-session8.mjs` — run the vite dev server, then from `demo/`: `node e2e-session8.mjs ../e2e-shots`). Production installer rebuilt at `src-tauri/target/release/bundle/nsis/Flightdeck_0.1.0_x64-setup.exe`.
eleaseundle
sis\Flightdeck_0.1.0_x64-setup.exe`.

**How to continue:** the pool is BACKLOG §M (UI-100..249) + §K, struck-through = done. Nothing is half-finished — every commit is green and pushed to main. Highest-value items still open: split-diff view (UI-165), partial merge (UI-168), board↔PR linkage (UI-157/159/163), in-app file preview (UI-209), fs watcher (UI-213), in-app browser (UI-6), agent run-history (UI-7), markdown panes (UI-8), onboarding tour (UI-9).

**REMAINING (next up):** ~115 of the §M/§K pool still open — the bigger ones: split-diff + word-level diff (UI-165/166), partial merge (UI-168), board↔PR linkage (UI-157/159/163), Settings search/export/reset (UI-180/182/183), restore-point + backup UI (UI-191/192), in-app file preview (UI-209), fs watcher (UI-213), React.memo pass (UI-226), token context-limit colours (UI-231), vendor glyphs (UI-236), manifest JSON Schema (UI-238). Then Phase 3 leftovers (presets, run-history, auto-name), R6 visual verify, and Phase 4 gated items (signing cert etc.). **E2E for Balu (session 8):** isolated workspace on a Node repo → panes run `npm ci` then the agent; Create PR on a GitHub repo → compare page opens; Ctrl+Shift+A opens the attention queue; token pill on Claude panes; Settings > Agents signed-in chips. Daily-driver installer: rebuild after the next batch (current one at `src-tauri\target\release\bundle\nsis\` predates batch 3).

Spike passed → building the real cockpit. Order:
1. **Cockpit UI to match the mock** — [BUILT 2026-07-19, compiles clean]. Zustand store (`store.ts`), `NewWorkspace` dialog (layout tiles + directory + agent checklist), `Cockpit` (topbar + workspace rail + `Sidebar` Explorer/Panes + pane grid), `PaneView` (dot-status header + branch pill), `Terminal` (xterm↔PTY). `fs_list_dir` Rust command for the Explorer. Frontend (tsc+vite) and cargo both exit 0. NOT yet run in the GUI this build — relaunch `npm run tauri dev` to view. Still inside this task: native folder picker, nested tree + actions, per-vendor state detection beyond running/idle.
1b. **DONE 2026-07-19**: per-pane agent selection in New Workspace (explicit mix, e.g. 3 Claude + 3 agy at the 6-layout), native folder picker (tauri-plugin-dialog), `BACKLOG.md` (local LLM via LM Studio/Qwen design + models-later note). Confirmed: multiple same-vendor panes work (no engine cap).
1c. **DONE 2026-07-19**: live per-pane status — reader emits `pty://state` "running" on output, a monitor thread emits "waiting" after 3s quiet; frontend updates the pane dot + roster. Activity-based v1 (vendor-agnostic).
1d. **DONE 2026-07-19 (session 2)**: multi-workspace (stacked, run in background, switch via left panel — panes stay MOUNTED so PTYs keep running when you switch); per-pane working directory (set in New Workspace, default = workspace dir, overridable via folder picker — because agents root their project at launch cwd, cd-in-terminal won't re-root them); notification bell (top bar: badge + dropdown of waiting panes, click to jump) + amber dot on background workspaces needing you; collapsible left side panel (Workspaces + Board, toggle in top bar); resizable split panes (`react-resizable-panels` v2 — pinned; v4 has a different API — drag any divider); Kanban Board (built by frontend subagent → `Board.tsx`/`Board.css`, 4 cols, drag cards, +New Task). Dropped per Balu: BridgeSwarm, billing, accounts. Store fully rewritten to multi-workspace (`store.ts`); old `Sidebar.tsx` removed.
1e. **DONE 2026-07-19 (session 2b)** — Rust: agy sticky workspace-trust (`ensure_agy_trust` writes each pane's folder into `~/.gemini/antigravity-cli/settings.json` `trustedWorkspaces` under an in-process lock → agy runs in ANY folder + multiple agy panes are safe); no-orphan shutdown (`pty_kill` now `taskkill /PID <pid> /T /F` the whole process tree, since Node CLIs spawn children). Terminal xterm theme updated to HUD palette. **DONE via 2 frontend subagents**: (a) full HUD theme overhaul (deep navy #0a0e15 + instrument cyan #22cfe0, replacing magenta) + custom SVG icon set (`Icons.tsx` — 14 line icons incl. radar-beacon `IconBell`, slider `IconSettings`, `IconBrand`) + premium side-panel redesign (monogram tiles, accent active bar + glow, pulsing waiting dot); (b) Kanban polished (real drag ghost, drop-target highlight, inline task composer, card-in animation) + seeded with the REAL Flightdeck build items across all 4 columns. `.btn-primary` is dark-on-cyan for contrast. Integrated build clean, relaunched. Not yet run-verified in the WebView — Balu to eyeball.
2. Vendor adapter trait + registry in Rust (generalise `build_command`). Pattern-based per-vendor detection (add "auth-required"; refine waiting) on top of the activity heuristic. [agy sticky-trust DONE in 1e.]
3. First-run CLI detection (binary probe) + folder open (`dialog.open`) wired to the launcher.
4. SQLite store (schema = architecture §3): workspaces/panes/sessions/scrollback (batched flush ~4KB/2s)/presets/events. Restore on reopen.
5. Windows shutdown (Ctrl+C → Job Object), crashed-pane relaunch, hidden-pane render throttle.
6. Tauri capability manifest hardening.
7. Kanban → pane dispatch: dropping a card into "In Progress" should spawn/assign an agent pane to that task (`Board.tsx` has the `// TODO: dispatch this card to an agent pane` marker). Currently the Board is standalone.
8. Settings screen: Appearance (multiple themes), Terminal, Agents, Shortcuts (NO Account/Billing). The ⚙ button in the top bar is a placeholder.
9. Explorer file-tree: re-add as a panel/section (dropped when the left panel became the workspace switcher). Rust `fs_list_dir` command still exists to build on.
Then Phase 1.5: `kimi-code` adapter (activate when Balu subscribes).

Run/dev command (from project dir, cargo not on global PATH):
`$env:Path="$env:USERPROFILE\.cargo\bin;$env:Path"; npm run tauri dev`

## Watch-list / likely first bugs

- Tauri v2 maps JS camelCase args to Rust snake_case: `Pane.tsx` invokes with `paneId` → Rust `pane_id`. If a command errors with a missing arg, that mapping is the cause — switch the JS keys to snake_case.
- `claude` via `pwsh -Command claude`: if the TUI doesn't render/accept input, try `cmd.exe /c claude` or invoking the `claude.cmd` shim directly.
- `cargo build` needs `../dist` to exist (frontendDist); the build job runs `npm run build` first to create it.

## Key files

- `D:\Dev\ai\research\flightdeck-build-spec.md` — the build, task by task.
- `projects\active\flightdeck\src-tauri\src\lib.rs` — PTY host (spike).
- `projects\active\flightdeck\src\Pane.tsx`, `src\App.tsx` — UI (spike).

## Resume

Prepend `%USERPROFILE%\.cargo\bin` to PATH. Read the build spec, then this STATE. Continue from the first unchecked Next step. Update this file after each meaningful step.
