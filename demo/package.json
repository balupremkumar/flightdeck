{
  "name": "flightdeck-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone motion-graphics demo reel for Flightdeck — records reel.html to a .webm via Playwright.",
  "scripts": {
    "record": "node record.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0"
  }
}
